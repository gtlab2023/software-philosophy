# Safe Refactoring Workflow

1. 声明模式：`behavior-preserving-refactor`、`feature-design` 或 `mixed-change`。
2. 定义可观察行为：输出、错误、状态变化、时序、协议、持久化和性能约束。
3. 按风险选择安全网：现有测试、特征测试、类型检查、编译器或受控迁移验证。
4. 每次只做一个可解释、可回退的结构步骤。
5. 每一步运行最窄的相关验证；失败时回退，不在失败状态继续叠加修改。
6. 混合任务先完成结构阶段并记录基线，再进入功能阶段。
7. 最终报告行为是否变化、验证证据、剩余风险和回滚点。
