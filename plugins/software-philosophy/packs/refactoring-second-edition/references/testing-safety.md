# Testing Safety Policy

- **低风险**：工具可证明的局部重命名、移动或格式无关调整。允许不新增测试，但必须运行编译、类型检查或现有测试。
- **中风险**：跨函数/模块移动、条件重组、数据结构调整。需要相关自动测试；缺失时先补特征测试。
- **高风险**：并发、事务、持久化迁移、协议、公共 API、错误语义或性能关键路径。需要可执行基线、明确回滚方案和约束验证。
- 无法建立充分安全网时，缩小范围、提高人工审查等级，并显式报告风险；不得把“看起来等价”当作验证证据。
